# BRAND — نگاشت برند به Design System

**Milestone 1** · منبع حقیقت: `brand-ghatehresan` (کتاب برند نسخهٔ ۱٫۰).
اگر هر مقداری در این مخزن با کتاب برند اختلاف داشت، **کتاب برند برنده است**.

---

## ۱. خط لولهٔ توکن‌ها

```
کتاب برند (brand-book.html :root + رنگ-های-برند.txt)
        │ استخراج دستی + راستی‌آزمایی
        ▼
resources/tokens/design-tokens.json     ← منبع حقیقت ماشین‌خوان
        │ نگاشت ۱:۱ (دستی، راستی‌آزمایی خودکار)
        ▼
resources/css/app.css (@theme)          ← کلاس‌های bg-navy، text-steel، ...
        │ Tailwind v4
        ▼
Blade components / views                ← مصرف‌کننده
```

**نگهبان ضد انحراف:** `tests/Unit/BrandTokensTest.php` بررسی می‌کند که
**تک‌تک** رنگ‌های `design-tokens.json` در `@theme` حضور دارند.
تغییر برند = به‌روزرسانی هر دو فایل + سبز ماندن تست.

## ۲. رنگ‌ها

| توکن | مقدار | نقش | کلاس‌ها |
|---|---|---|---|
| `navy` | `#0E2A47` | اصلی — سایدبار، هدر، تیتر | `bg-navy text-navy border-navy` |
| `navy-2` | `#16375A` | hover سرمه‌ای (مشتق) | `hover:bg-navy-2` |
| `orange` | `#F26B1D` | تأکید — فقط کنش اصلی | `bg-orange text-orange` |
| `orange-d` | `#D25510` | hover نارنجی (مشتق) | `hover:bg-orange-d` |
| `ink` | `#111820` | متن اصلی | `text-ink` |
| `steel` | `#55616E` | متن فرعی | `text-steel` |
| `line` | `#E8EBEF` | مرز، جداکننده | `border-line` |
| `paper` | `#F9FAFB` | پس‌زمینهٔ صفحه | `bg-paper` |
| `success/danger/warning/info` | `#1F9D55/#D93025/#F0A202/#2563EB` | کارکردی | `bg-success/10 text-success-d` و… |

### قوانین سخت (نقض = بازبینی رد می‌شود)

1. **قاعدهٔ ۶۰/۳۰/۱۰:** ۶۰٪ خنثی · ۳۰٪ سرمه‌ای · **حداکثر ۱۰٪ نارنجی**.
2. **نارنجی رنگ متن نیست:** متن ریز نارنجی روی سفید ممنوع؛ نارنجی فقط
   دکمهٔ کنش اصلی، آیتم فعال سایدبار، فلش لوگو.
3. **رنگ جدید اختراع نکن:** هر هگز خارج از `design-tokens.json` ممنوع
   (استثنا: مشتقات مستندشده با پسوند `-d` و متن سایدبار).
4. **کنتراست:** سرمه‌ای/سفید AAA · نارنجی/سرمه‌ای AA ·
   نارنجی/سفید فقط تیتر درشت (‎≥24px).

## ۳. تایپوگرافی

- **فونت فارسی:** Vazirmatn ‏(۴۰۰/۵۰۰/۷۰۰/۹۰۰) — خودمیزبانی در `/fonts`، هرگز CDN.
- **فونت لاتین/فنی:** Inter برای کدها (`EMP-0142`) — کلاس `font-en`.
- **تراکم ابزار داخلی:** بدنه `14.5px/1.85` (از سامانهٔ مدیریت)؛
  صفحات محتوایی `16.5px/1.9` (از کتاب برند).
- **اعداد:** مبالغ/تاریخ/شمارش فارسی (۱۲٬۵۰۰)؛ کدها و شناسه‌ها لاتین.
- **نیم‌فاصله:** «قطعه‌رسان»، «می‌رسونیم» — همیشه ZWNJ.

## ۴. لوگو

فایل‌ها در `public/brand/` (کپی از مخزن برند):

| فایل | کاربرد |
|---|---|
| `logo-primary.svg` | پیش‌فرض (عمودی) |
| `logo-horizontal.svg` | هدر و سربرگ |
| `logo-icon.svg` | فاوآیکون و آیکون (استفادهٔ فعلی در لی‌اوت) |
| `logo-mono.svg` | تک‌رنگ (رنگ با CSS) |

> **اصلاح اِعمال‌شده:** بلوک `<style>@import …fonts.googleapis…</style>`
> از SVGها حذف شد، چون کتاب برند CDN خارجی را ممنوع کرده و گوگل‌فونت
> در ایران قابل اتکا نیست. نام‌نویس با فونت محلی سیستم رندر می‌شود؛
> نسخهٔ outlineشده در صورت نیاز از طراح گرافیک گرفته شود.

محدودیت‌ها: حریم امن = نصف ارتفاع مهره · حداقل 120px (کامل) / 32px (نماد) ·
بدون کشیدگی، چرخش، سایه و تغییر رنگ.

## ۵. کامپوننت‌ها (M1)

| کامپوننت | فایل | نقش |
|---|---|---|
| شل RTL + سایدبار | `components/layouts/app.blade.php` (`<x-layouts.app>`) | سایدبار ۲۵۲px راست، drawer موبایل |
| سربرگ صفحه | `components/page-head.blade.php` | تیتر + زیرعنوان + اکشن‌ها |
| کارت | `components/card.blade.php` | کانتینر استاندارد بخش‌ها |
| بج وضعیت | `components/badge.blade.php` | ۵px radius، متن ۱۲px بولد (طبق برند) |
| دکمه | `components/btn.blade.php` | default/primary/navy/danger ‏(+sm) |
| آمار | `components/stat.blade.php` | KPI با tone مثبت/منفی |
| حالت خالی | `components/empty-state.blade.php` | آیکون + تیتر + اکشن اختیاری |
| پیام فلش | `components/flash.blade.php` | success/danger/warning/info + خطاهای فرم |

سبک آیکون: خطی (outline)، ضخامت ~۲px، شبکهٔ ۲۴ — سرمه‌ای؛
نارنجی فقط آیکون فعال.

## ۶. لحن رشته‌های UI

خودمانی اما دقیق (از کتاب برند §۰۳): «۴ عدد موجوده» نه «موجود»؛
«الان نداریم، ۳ روز دیگه می‌رسه» نه «تماس بگیرید».
بدون ادعای بی‌پشتوانه («بهترین») و بدون لحن اداری خشک («بدینوسیله…»).

## ۷. نام محصول

HRM برند مستقل نیست (معماری Master-brand):
**«منابع انسانی قطعه‌رسان»** (`config/hrm.php` → `product_name`).
