# SECURITY — امنیت HRM

**Milestone 2** · این سند کنترل‌های الزامی را تعریف می‌کند.
§۲ و §۳ در M2 پیاده‌سازی شدند (جزئیات: `AUTHENTICATION.md` و `AUTHORIZATION.md`)؛
هر کنترل یک تست امنیتی متناظر دارد (`TESTING.md`).

---

## ۱. مدل تهدید (خلاصه)

| دارایی | مهاجم | بردار |
|---|---|---|
| حقوق، کد ملی، حساب بانکی، قرارداد، مدارک | کاربر داخلی کنجکاو/ناراضی | دست‌کاری URL/ID، جست‌وجو، خروجی گزارش |
| نشست‌ها و اعتبارنامه‌ها | مهاجم خارجی | Brute-force، Session hijack، XSS |
| کل دیتابیس | مهاجم خارجی | باگ احراز هویت، دسترسی مستقیم فایل، بکاپ محافظت‌نشده |
| یکپارچگی Audit | ادمین خاطی | حذف/ویرایش لاگ |

**پیش‌فرض:** هر ورودی کاربر مخرب است؛ هر خروجی باید فیلترشده باشد؛
هر عملیات حساس باید Audit شود؛ هیچ‌چیز امنیتی «فقط در Frontend» نیست.

## ۲. احراز هویت (پیاده‌سازی M2 — جزئیات در `AUTHENTICATION.md`)

| کنترل | الزام |
|---|---|
| Hash رمز | **Argon2id**؛ fallback قابل‌قبول bcrypt (۱۲ راند). انتخاب نهایی در M2 روی XAMPP هدف راستی‌آزمایی می‌شود |
| سیاست رمز | حداقل ۱۲ کاراکتر + بررسی `zxcvbn`؛ جلوگیری از رمزهای تکراری/معروف |
| MFA | TOTP (RFC 6238) + کدهای بازیابی؛ **اجباری** برای super-admin/hr-admin/finance |
| نشست | کوکی `HttpOnly + Secure + SameSite=Lax`؛ چرخش ID پس از ورود؛ انقضای مطلق |
| قفل حساب | ۵ تلاش ناموفق → قفل تدریجی + اعلان به کاربر + ثبت Audit |
| Rate limit ورود | ۵ درخواست/دقیقه/IP (سخت‌گیرانه‌تر از سایر مسیرها) |
| بازیابی رمز | توکن یک‌بارمصرف + انقضای ۶۰ دقیقه + ابطال پس از استفاده |

## ۳. مجوز (پیاده‌سازی M2 — جزئیات در `AUTHORIZATION.md`)

- **سه‌لایهٔ اجباری در Backend:** Middleware (مسیر) → Policy (رکورد) → Query Scope (مجموعه).
- Frontend فقط دکمه/منو را پنهان می‌کند — **هرگز** جایگزین کنترل Backend نیست.
- Scopeها: `self/team/department/company/all` — از روی درخت سازمان محاسبه می‌شوند.
- همهٔ بررسی‌های M46 (`TESTING.md` §۵) باید سبز باشند وگرنه انتشار ممنوع.

## ۴. دادهٔ حساس

| داده | نگهداری | خروجی |
|---|---|---|
| کد ملی | ستون رمزنگاری‌شده (AES-256-GCM سطح کاربرد) | فقط با `employee.national_id.view` |
| حقوق/مزایا | جدول جدا `compensations` | فقط با `employee.salary.view`؛ هرگز در لیست‌ها |
| حساب بانکی | ستون رمزنگاری‌شده | فقط با `employee.bank.view` |
| قرارداد/مدارک | دیسک خصوصی + URL امضاشده | هر دانلود ثبت Audit می‌شود |
| ارزیابی عملکرد | جدول جدا | فقط HR/مدیر مستقیم/خود کارمند (self-review) |
| لاگ‌ها | **هیچ دادهٔ حساسی در لاگ** — رمز، توکن، کد ملی، حقوق ممنوع |

**ضد over-fetching:** Resourceها فیلد حساس را به‌طور پیش‌فرض حذف می‌کنند؛
نمایش فقط با Permission صریح (+ پارامتر `?include=` برای موارد مجاز).

## ۵. فایل‌ها (پیاده‌سازی M8)

- دیسک `hrm_private` → `storage/app/private` — **خارج از webroot**، بدون symlink عمومی.
- نام فایل تصادفی؛ نام اصلی فقط در DB.
- اعتبارسنجی: magic bytes (نه پسوند)، سقف اندازه از `document_types`، MIME مجاز.
- دانلود فقط با **URL امضاشدهٔ موقت** (انقضای کوتاه) + بررسی Permission + ثبت Audit.
- آپلود هرگز با دسترسی اجرایی ذخیره نمی‌شود؛ اسکن محتوا در صورت امکان.

## ۶. وب و زیرساخت

| کنترل | الزام |
|---|---|
| HTTPS | اجباری در staging/production + HSTS |
| هدرها | `X-Frame-Options: DENY` (یا frame-ancestors در CSP) · `X-Content-Type-Options: nosniff` · `Referrer-Policy` · CSP سخت‌گیرانه (بدون `unsafe-inline` — با nonce در صورت نیاز Alpine) |
| CSRF | توکن روی همهٔ درخواست‌های state-changing (Blade: `@csrf`) |
| XSS | فرار خودکار Blade (`{{ }}`)؛ `{!! !!}` **ممنوع** مگر با sanitize صریح و کامنت دلیل |
| SQLi | فقط Eloquent/Query Builder پارامتری؛ SQL خام بدون binding **ممنوع** (بررسی در بازبینی کد) |
| Mass assignment | `$fillable` صریح (یا attribute مربوطه)؛ `$guarded=[]` ممنوع |
| Validation | FormRequest برای هر endpoint؛ whitelist نه blacklist |
| Secrets | فقط `.env` (خارج از مخزن)؛ `.env.example` بدون مقدار واقعی؛ چرخش APP_KEY طبق رویه |
| Debug | `APP_DEBUG=false` در staging/production؛ صفحهٔ خطای عمومی بدون stack trace |
| CORS | پیش‌فرض بسته؛ فقط در صورت نیاز SPA جدا باز می‌شود (در scope نیست) |

## ۷. پشتیبان و بازیابی (پیاده‌سازی M12 — جزئیات در `BACKUP.md`)

- DB + فایل‌ها، **رمزنگاری‌شده**، به مقصد **خارج از سرور** (الزام M38).
- نگهداری: روزانه ۱۴ · هفتگی ۸ · ماهانه ۱۲.
- تست بازیابی ماهانه (بازیابی روی staging) — بکاپ تست‌نشده = بکاپ نداریم.

## ۸. وابستگی‌ها و زنجیرهٔ تأمین

- Dependabot هفتگی (composer/npm/actions) + `composer audit` و `npm audit` در CI.
- `composer install` با `--prefer-dist`؛ قفل‌فایل‌ها (`composer.lock`، `package-lock.json`) در مخزن.
- `.npmrc`: `ignore-scripts=true` (جلوگیری از اجرای اسکریپت نصب مخرب).
- به‌روزرسانی امنیتی Laravel در **۷۲ ساعت** اول انتشار.

## ۹. چک‌لیست امنیتی هر انتشار

```
□ همهٔ تست‌های Permission (M46) سبز
□ pint + phpstan بدون خطا
□ composer audit و npm audit بدون یافتهٔ high/critical
□ APP_DEBUG=false و APP_KEY یکتا در production
□ DocumentRoot روی public/ (تست دسترسی مستقیم به /.env → 403/404)
□ بکاپ اخیر + تست بازیابی معتبر
□ لاگ‌ها فاقد دادهٔ حساس (نمونه‌برداری)
□ هدرهای امنیتی فعال (بررسی با curl)
□ Rate limit روی /login و مسیرهای عمومی فعال
```

## ۱۰. گزارش آسیب‌پذیری

یافته‌های امنیتی **در Issue عمومی ثبت نشوند**.
مسیر گزارش خصوصی در `DEPLOYMENT.md` (M12) اعلام می‌شود.
تا آن زمان: گزارش مستقیم به مالک مخزن.
